enum FilterOperator {
  EQUALS = "is",
  NOT_EQUALS = "is not",
  CONTAINS = "contains",
  NOT_CONTAINS = "doesn't contain",
  INCLUDES = "includes",
  NOT_INCLUDES = "doesn't include",
}

export { FilterOperator };
